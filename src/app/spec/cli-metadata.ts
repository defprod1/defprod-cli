import { rootCommand } from './root.command';

/**
 * Export the root command for use in documentation generation.
 * This allows the backend to access the CLI command tree without
 * importing the entire CLI application.
 */
export { rootCommand };

